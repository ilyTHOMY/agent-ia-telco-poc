services:
  backend:
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
    environment:
      - ENV=development
      - LOG_LEVEL=debug

  frontend:
    image: nginx:alpine
    volumes:
      - ./frontend:/usr/share/nginx/html
    ports:
      - "3000:80"
